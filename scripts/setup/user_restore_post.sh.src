#!/bin/bash
SCRIPT="/usr/local/directadmin/plugins/postgresql/exec/hooks/user_restore_post.sh";  # PostgreSQL
test -x "${SCRIPT}" && "${SCRIPT}" "${username}" "${reseller}" "${filename}"; # PostgreSQL
